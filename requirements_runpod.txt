# ADMET Challenge - RunPod RTX 4090 Requirements
# Install with: uv pip install -r requirements_runpod.txt
#
# For maximum performance on RTX 4090:
#   uv pip install flash-attn --no-build-isolation

# ============================================================================
# CORE (Required)
# ============================================================================

# Data handling
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0

# Progress & Utilities
tqdm>=4.66.0
rich>=13.0.0

# ============================================================================
# CHEMISTRY (Required)
# ============================================================================

rdkit>=2023.9.1

# ============================================================================
# GRADIENT BOOSTING - GPU Accelerated (Required)
# ============================================================================

xgboost>=2.0.0      # Uses device='cuda'
catboost>=1.2.0     # Uses task_type='GPU'
lightgbm>=4.0.0     # CPU fallback (GPU build optional)

# ============================================================================
# DEEP LEARNING (Required)
# ============================================================================

torch>=2.0.0        # PyTorch 2.0+ for torch.compile
lightning>=2.0.0    # PyTorch Lightning

# ============================================================================
# PHASE 2: TRANSFORMER EMBEDDINGS (Recommended)
# ============================================================================

# ChemBERTa - 768-dim molecular embeddings
# Optimized with: Flash Attention 2, BF16, torch.compile
transformers>=4.36.0
huggingface_hub>=0.19.0

# ============================================================================
# PHASE 2: MESSAGE PASSING NEURAL NETWORK (Recommended)
# ============================================================================

# Chemprop D-MPNN - Graph neural network for molecules
chemprop>=2.0.0

# ============================================================================
# OPTIONAL: MAXIMUM PERFORMANCE
# ============================================================================

# Flash Attention 2 - 2x faster transformers on RTX 4090
# Install separately (requires CUDA toolkit):
#   uv pip install flash-attn --no-build-isolation

# Uni-Mol 3D Features - 512-dim 3D conformer embeddings
# Install separately:
#   uv pip install unimol_tools

# ============================================================================
# QUICK INSTALL COMMANDS
# ============================================================================
#
# Basic (GBDT only):
#   uv pip install numpy pandas scipy scikit-learn rdkit xgboost catboost lightgbm torch tqdm
#
# Full Phase 2:
#   uv pip install -r requirements_runpod.txt
#
# Maximum Performance:
#   uv pip install -r requirements_runpod.txt
#   uv pip install flash-attn --no-build-isolation
#   uv pip install unimol_tools
